<div class="loadingSpinner flex justify-center">
  <div
    class="loadingSpinner__inner animate-spin h-10 w-10 border-4 rounded-full border-t-transparent"
  >
  </div>
</div>

<style lang="scss">
  @use '../../styles/utils' as utils;
  @use '../../styles/variables' as vars;

  .loadingSpinner {
    display: flex;
    justify-content: center;

    &__inner {
      @include utils.spin();
      width: 40px;
      height: 40px;
      border-radius: 50%;
      border: 4px solid #fff;
      border-top-color: transparent;
    }
  }
</style>
