<div class="loadingSpinner">
  <div class="loadingSpinner__inner"></div>
</div>

<style lang="scss">
  @use '../../styles/utils' as utils;
  @use '../../styles/variables' as vars;

  .loadingSpinner {
    display: flex;
    justify-content: center;

    &__inner {
      @include utils.spin();
      width: 40px;
      height: 40px;
      border-radius: 50%;
      border: 4px solid #fff;
      border-top-color: transparent;
    }
  }
</style>
