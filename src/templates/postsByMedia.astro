---
import React from 'react'

import Head from '../components/Head'
import Pagination from '../components/Pagination'
import PostList from '../components/PostList'
import PostListLayout from '../components/PostListLayout.astro'
import { group } from 'console'
import type { Frontmatter, Media } from '../types'
import type { Page } from 'astro'
import SideBar from '../components/SideBar.astro'

type props = {
  page: Page<Frontmatter>
  media: Media
}
const { page, media } = Astro.props
---

<div class="postsByMedia__tagText">
  <b>#{media === 'owned' ? 'mimu-memo' : media}</b>
  の記事
</div>
<div class="postsByMedia__body">
  <PostList posts={page.data} />
  <Pagination page={page} />
</div>

<style lang="scss">
  @use '../styles/mixin' as mixin;
  @use '../styles/variables' as vars;

  .postsByMedia {
    &__tagText {
      text-align: center;
      font-size: 20px;

      & b {
        font-weight: bold;
      }
    }

    &__body {
      display: flex;
    }
  }
</style>
